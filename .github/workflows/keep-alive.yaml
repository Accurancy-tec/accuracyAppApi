name: Keep Render Awake

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping na API
        run: |
          curl --fail --silent --show-error \
            https://accuracyappapi.onrender.com/tests/health.php
